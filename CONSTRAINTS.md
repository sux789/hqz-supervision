# 硬约束注册表

> 规则：新约束递增编号；每条标注来源版本；被反转的旧约束**不删**，标注「已被 Cxx 反转」；冲突时按版本号判新旧。AI 改代码前必读。

| 编号 | 约束 | 来源版本 | 反转链 |
|---|---|---|---|
| C01 | 数据 sheet 的展示/编辑/拍照/搜索行为一律由「参数」sheet 驱动，代码禁止硬编码照片命名、目录、可编辑列、搜索控件等约定（hqz-survey 的历史教训：buildPhotoName/photoSaveSubdir 硬编码于 app.js） | v0.1 | — |
| C02 | 参数 sheet 语法固定：表头 `key\|value\|类型\|默认值\|说明\|示例`；多值 `;`、项内子字段 `\|`、目录 `/`、文件名段 `_`、占位符 `{{}}`；解析器遇到未知 key 或违规分隔符必须报错（带行号），不静默跳过 | v0.1 | — |
| C03 | 不得影响线上其他业务：不修改 hqz-survey/hqz-cam-app 代码与后端；本项目暂不部署（本地预览），将来部署须独立 appId/路径，三者互不重复互不干扰 | v0.2 | — |
| C04 | 通用工具定位：不内置任何调查专属业务（样地/GDB/打卡业务等），一切业务行为由「参数」sheet 驱动；前端积木复用 hqz-survey 验证过的 jspreadsheet-ce@4 + jsuites，后端业务接口不复用 | v0.2 | 反转 doc/002 初版「整功能迁移」叙事 |
| C05 | 账号与安全：管理员 雷华雄（lhx123，sha256 存储，SQLite users 表）可访问前端+后台 /admin；后台接口必须校验 admin 角色；密码不得明文出现在代码/日志 | v0.3 | — |
| C06 | 水印内容不参数化：水印首行=拍摄时刻时间戳 `yyyy-MM-dd HH:mm:ss`（本地时区，前端固定行为），正文=「相片备注」模板渲染，均不随模板变化 | v0.9.4 | 修订 v0.3「日期 YYYY-MM-DD」→ 完整时间戳（秒级） |
| C07 | 相片不在服务器长期存储：拍照仍优先存本地（App 存系统相册 / 浏览器下载），无预览（v0.8.1 起仅显示完整文件 path）；云同步开启时经服务器中转（/api/photo），七牛 zz-1 暂存 + data/pending 缓冲 → 百度网盘，百度成功即删 pending，七牛副本按保留期（默认 30 天）清理 | v0.9 | 修订 v0.8「完全不落服务器」：为可靠性引入中转+暂存，但成功即删不长期存储 |
| C08 | 导出必须按上传模板回填：上传时原样留存 source.xlsx(.xlsm)，导出=原模板清数据行后回填，禁止白板重建丢格式；静态 JS 引用必须带 ?v=版本号 熔断缓存 | v0.8 | 反转 v0.4 openpyxl 白板重建导出 |
| C09 | 视频只存手机本机、不上传：App 内「🎬 视频」直接在页面录制（MediaRecorder 按后台「视频压缩」参数边录边压，体积小），停止后**仅保存到手机**（原生 saveVideo→Movies 相册【需新版 APK】/ saveFile→下载目录 / 浏览器下载），并在本机记录路径提示；不调用任何上传接口、不入七牛/百度、服务器无副本；**容器只认 MP4/H.264**（本机不支持则改走系统相机录制，保证可播放） | v0.11 | 反转 v0.10.1「手机端压缩后上传云盘」 |
| C10 | 压缩与同步解耦：图片压缩、视频压缩、云同步是三件独立的事，后台分三块各自保存；压缩在手机端/本地完成（关掉同步也照常压缩），同步只决定照片是否上云（视频永不上云）。图片压缩参数优先级：工作簿「参数 sheet」(压缩最长边/压缩质量) → 后台「图片压缩」默认(photo_max_side/photo_quality) → 内置兜底；视频压缩参数全部来自后台（录制方式/录制质量/最长边/码率/帧率/单段时长/转码开关） | v0.16 | 修订 C01「一律由参数 sheet 驱动」：图片压缩增加服务器级缺省（sheet 仍优先） |
| C11 | 唯一键与通用化（v0.23）：①**数据 sheet 定位**——优先精确取名为 `data` 的 sheet（此时多余 sheet 可容忍），否则要求"有且仅有 1 个非「参数」sheet"（旧模板兼容），否则**明确报错**，禁止静默取第一个；②**行标识**（照片归档目录、变更日志、导出定位）一律取参数 `unique-key` 声明的列，未声明才回落 `小班号`，代码与前端禁止再硬编码「小班号」；③`unique-key` 只认这个英文名，值必须是数据表真实表头、只能一个列名、**不得出现在「可编辑列」**中；④声明后该列**必须非空且唯一**，违反则拒绝上传并报错带 Excel 行号；**未声明则不校验**（旧模板零改动）；⑤审计字段由参数 `日志字段` 驱动，百分比列由参数 `百分比列` 驱动（缺省回落旧规则），**且前端「结果联动」的字段必须复用后端下发的 `log_fields`，不得再写死一份**；⑥必填 key 按需化——`可编辑列`/`功能` 恒必填，`相片备注`/`相片文件名`/`目录` 仅在「功能」含 拍照/视频 时必填；⑦删除工作簿必须连带清理 `data/workbooks/<id>/`（源模板）与 `accept_logs`/`photo_sync` 关联行。总目标：**同一套 App 装不同类型的 Excel，尽量不改代码** | v0.23 | 反转 v0.1 起的隐含约定「数据 sheet = 第一个非参数 sheet」「行标识恒为小班号」「拍照三件套恒必填」 |
| C12 | 多应用同域部署（gateway）时，**应用内一切跳转/回退/下载地址必须用 `window.SUP_BASE`（= Flask 注入的 `request.script_root`）锚定本应用，禁止硬编码 `'/'` 或裸路径**。`'/'` 是网关首页（所有应用的入口），用户会被丢进别的应用、再拿本应用账号反复试密码（2026-09-14 何明星「退出」实测踩此坑）。静态 JS 改动后必须提 `?v=` 版本号熔断（C08）。排查"登录不上"类问题时**先看访问日志有没有失败的登录请求**：一条都没有 ⇒ 请求未到服务器 ⇒ 问题在客户端/落点，不在鉴权 | v0.23.1 | — |
| C13 | 可见性与权限必须分开（v0.24）：①**「下架」是软删除**——`workbooks.is_active=0` 只让 **App/user 端看不见、不能操作**（列表不列、读/写/导出/拍照/录像一律 403），数据行/源 Excel/日志/照片全部保留，管理员始终放行且可「恢复」；彻底删除只走 `DELETE ?purge=1` 且只暴露在已下架列表里。②**前端隐藏一律不构成权限**：真正的边界只在服务端的 `@login_required`/`@admin_required`；**禁止**用 URL 参数（如 `?showdel=1`）当权限或"危险模式"开关——它会因被存书签而常开、会随链接扩散、会被后来人误当成权限，属于假安全。③**不可逆操作必须要求照着输入对象名称确认**（不是一键 confirm），且用自建 DOM 弹层而非 `window.prompt`（Android WebView 默认不实现 `onJsPrompt`，手机上会失效）。④**标志位为 0 时不得写在 `or` 左侧**（`x or 1` 会把 0 吃成 1，导致守卫永久失效） | v0.24 |  —— |
| C14 | 保存与离场纪律（v0.25）：①**任何防抖/节流保存都必须能在离场前被冲刷**——点返回、路由切换、切后台、关页/重载四条路径全都要调 `flushRowSave()`，卸载时用 `fetch(keepalive:true)`（**禁用 `sendBeacon`**：它带不了 `X-Sup-Token` 头，只能把令牌塞 URL → 落进访问日志）。②待保存任务必须**快照 wid/ridx/values**，禁止延后到触发时才按 `cur/curIdx` 取值（切了工作簿会把 A 的数据写进 B）。③编辑器（jspreadsheet）的 `onchange` 只在 blur/Enter 触发，因此必须**另加逐键捕获**（委托 `input`）把最新输入同步进内存，否则离场冲刷上去的是旧值。④`datetime`/`date` 判断**必须先 `datetime` 再 `date`**（datetime 是 date 子类，且 `str(datetime)` 会带 `00:00:00` 污染数据）。⑤后台「下载 Excel」的筛选**只筛行**，字段由参数 `导出筛选`（`字段\|select` / `字段\|date`）声明，弹框只显示**声明 ∩ 该工作簿表头**的字段（没有的字段不提示），下载接口也只认声明过的字段（多传的参数一律忽略）；下拉候选值必须**三路合并**（该列对应的「`<列>选项`」参数枚举 + 列名含「人/员」的列并入系统用户名单 + 数据里出现过的值），否则空数据时下拉是死的。⑥从登录页回跳要带 `?next=<原页面>`，登录后送回原页而非首页；非管理员则静默回 App 并清掉 `next` | v0.25 | — |
| C15 | 就地更新与合并（v0.26）：①**「更新 Excel」必须就地更新、保留原工作簿 id**（`accept_logs` 绑 workbook_id+row_idx、下架状态、App 使用习惯都靠它不断链），只有「上传」才新建。②合并**以既有行顺序为基准**：匹配的行**原地**合并（老行 row_idx 不变 → 变更日志不错位），新表的行**追加末尾**，旧表有而新表没有的行**保留不删**。③冲突取值：**只有「可编辑列」（旧∪新）的旧值非空优先**，**其余列以新表为准** —— 否则"填到一半改 Excel"（改原始数据、加列）永远不会生效。④按**列名**对齐（列可增删换序），输出列序=新表列序；数值类型必须保留（别把 108.87 变文本）。⑤**不设"重合比例"自动闸门**：改为「预览统计（匹配/新增/仅旧/冲突/被清空/被删列字段）+ 输入工作簿名确认」；`?dry=1` 只算不写。⑥**必须可回滚**：写库前自动备份 DB 行 + 源模板文件（`workbook_backups`，每簿最多 5 份），`POST /rollback` 还原。⑦唯一键列名两边不一致、或该列在任一侧缺失 → **拒绝更新**。 | v0.26 | 反转 v0.1 起「改模板只能重新上传（= 新建工作簿）」 |
| C16 | 前端脚本健壮性（v0.27）：**任何"可能不存在"的元素引用都要走 `on()` 或空值守卫，禁止顶层裸 `addEventListener`** —— 卡片/按钮被注释掉后，`$('#x')` 返回 `null`，`null.addEventListener` 抛 TypeError 会让**整个脚本加载失败、整页失效**（且症状是"所有按钮都没反应"，极易误判成后端问题）。删元素前先确认引用处已包 `on()`。同时：**隐藏一块 UI 要连它依赖的 JS 一起处理**（加载函数加 `if (!el) return;`），并同步提 `?v=`（C08） | v0.27 | — |
| C17 | 不可假定第三方组件的行为（v0.28）：①对编辑器/第三方库，**关键数据不得依赖"它会不会派发事件"** —— 混淆源码里无法确认时，就当作"不会派发"来设计；在离场/承载边界上**直接读 DOM** 取真实值（编辑器在编辑时取 `<input>.value`，否则取单元格文本）。②**关键写入必须有落盘草稿**：每次改动同步写 `localStorage`（同步写入，进程被杀也不丢），启动/切回前台自动补交，补交成功才清；国产 ROM 会在后台冻结定时器，因此"切回前台"也要触发补交。③**切后台时顺序必须是"先收值再冲刷"** —— 编辑器往往还没提交，不先收值等于什么都没存。④**验证不得自证循环**：自己 `createElement` 造输入再派发事件、然后观察自己的监听器，只证明"监听器存在"，**不能证明真实输入会触发它**；必须走真实交互路径（真实点击/键入）才算验证。 | v0.28 | — |

<!-- 示例行可删。第一条真实约束出现时开始填。 -->
