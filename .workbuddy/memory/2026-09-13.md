## 2026-09-13 v0.9.1 修复拍照 quality 报错
- 用户报：拍照报 "quality is not defined"。根因：v0.9 前端压缩参数化的 3 处编辑中，toBlob 行落盘了，但 drawWatermark 签名(opts)与调用点传参两处被 Edit 工具"报成功未落盘"（零宽字符路径 flaky 现象再次出现）→ 运行时 quality 未定义，拍照直接失败。
- 修复：改用 python 脚本补齐两处（assert in + replace，先 find 验证），grep + node --check 双验证；缓存 v=0.8.4；deploy 上线 curl 验证（页面 v=0.8.4、live app.js 含 opts.quality）。
- 教训固化：此项目路径含零宽字符，所有代码编辑必须"编辑后 grep 复核"；重要改动优先用 python 脚本 assert+replace 而非 Edit 工具。

## 后台线程化清理（00:25 后）
- 用户问：中转成功后清理会不会阻塞拍照响应。答：会（补推 1-2s/张、七牛删除 0.2-0.5s/个）→ 改造：try_push_pending + purge_expired 挪到 /api/photo 响应返回后的 daemon 线程（独立 sqlite 连接，worker 退出被杀无害，下次触发续做）；purge_expired 加 limit=20 单次上限分批。
- 本地实测：拍照响应 3.95s（仅本张两跳，与改造前持平），日志无异常，pending 即清。

## v0.9.3 压缩优化（01:35 后）
- 用户：先提交 → 按方案 A 加水印排版省比特。已提交积压（.gitignore 忽略 .playwright-cli、set_gh_secrets.sh 修正如实提交、记忆日志）8af4af6。
- 水印版式改为「半透明白底块(rgba(255,255,255,.62) 圆角) + 实心黑字」，取代白描边黑字（描边=高频边缘费比特）。用 playwright 渲染同底图对比实测：0.80 编码 53KB→49KB（省 7%），文字更清晰无彩边；截图确认版式/行序/位置正确。
- 方案 A：前端默认压缩参数 1600/0.85 → 1440/0.80（约省 40%）；模板 data/中幼林抚育-网格填表模板.xlsx 参数 sheet 补「压缩最长边=1440 / 压缩质量=0.8」两行（解析器验证通过，13 列 1173 行正常）；doc/001 升为 10 键 + 水印版式章节；doc/007 §6 默认值同步。
- 部署上线 v=0.8.5，curl 验证线上 app.js 含新版式且 PHOTO_MAX_SIDE=1440 / PHOTO_QUALITY=0.80。提交 8668e21。
- 再次踩到「python replace 前 assert 失败」的诡异现象（templates/index.html），改成先 find 定位再 replace 即成功——本项目路径含零宽字符，稳妥写法是 find+replace 并 grep 复核。

## v0.9.4 水印时间戳（02:08 后）
- 用户：水印日期改为时间 yyyy-MM-dd HH:mm:ss。实现：水印首行 `时间：2026-09-13 02:09:20`（本地时区，秒级，手写 pad 避免依赖 toLocaleString 的本地化差异）。
- playwright 渲染验证格式与版式正常；C06 由「水印日期 YYYY-MM-DD」修订为「水印内容不参数化：首行 yyyy-MM-dd HH:mm:ss」；doc/001 水印版式章节、STATE.md 同步；缓存 v=0.8.6；部署上线 curl 验证（提交 e06937b）。
- 现水印三行：时间 / 坐标 / 备注；文件名 {{时间}} 占位符仍为 YYYYMMDD_HHMMSS（未变）。

## v0.9.5 水印底块留白（02:14 后）
- 用户：水印换行了，适当加大背景。实现：内边距 fs×0.7/×0.5、行距 ×1.45、圆角 ×0.35、外边距 ×0.55；底块最小宽 = 画面 30%；备注每行 22→26 字（减少换行行数）；新增高度保护——底块超画面 55% 时整体按比例缩字号（下限 14px），超长备注完整入画不裁切。
- playwright 三档实测（短3行/中4行/超长6行自动缩字号）截图确认留白与完整性；提交 8f20e63，部署上线 curl 验证 v=0.8.7。
- 调试提示：自建 wm 测试页曾漏传 ctx 参数导致水印不绘制（页面静默失败）——产品代码无此问题（ctx 在同作用域），测试页须传 canvas/ctx/coords/remark/quality 五个参数。

## v0.10 视频功能（12:42 后）
- 用户：拍照与轨迹之间加视频按钮；视频压缩参数在后台设置（给默认值）；命名/目录类似拍照但文件名加"视频"；详情中视频与图片名称样式不一致。
- 架构决策：**浏览器/WebView 无法转码视频 → 压缩必须在服务器**（C09）。故 /api/video 接收原视频 → data/pending/{id}.mp4 → 后台 daemon 线程 ffmpeg 压缩（{id}_c.mp4）→ 七牛 → 百度；接收即返回（大文件不阻塞手机，实测 2.7MB 上传响应 7s）。
- ffmpeg 落地：服务器原本没有；johnvansickle 国内限速（40MB 下 9MB/10min）→ 改用 **npmmirror ffmpeg-static b6.0/ffmpeg-linux-x64.gz**（28MB 秒下），解压到 apps/supervision/bin/ffmpeg（deploy.sh 不覆盖 bin/）；含 libx264+aac。
- 后台参数（settings）：video_max_height 720 / video_crf 28 / video_maxrate_k 2500 / video_audio_k 96 / video_max_mb 300 / video_ffmpeg（空=自动探测 bin/ffmpeg）。
- 实测：1080p 3s 2742KB → 234KB（8.6%）；2s 1833KB → 158KB（8.6%），state=baidu_ok，pending 自动清空。
- **顺带修复**：百度 create 建目录即使带 rtype=1 仍会同名转存出 `目录名_YYYYMMDD_HHMMSS` 空副本（实测复现，共 3 个 YJ-1715_2026* 空目录）→ mkdir_p 改「先 list 父目录确认不存在再 create」（trans_v2 原做法），复测无副本产生；已清理历史空副本目录。
- 线上工作簿（id=3）config/param_rows 与 source.xlsx 参数 sheet 已同步 功能=拍照;轨迹;视频（无需用户重新上传）。
- 前端：工具栏 导出/拍照/🎬视频/轨迹/拍摄记录；拍摄记录条目区分——照片 📷+灰绿「照片」标签白底、视频 🎬+蓝色「视频」标签浅蓝底；弹层计数「照片 N · 视频 M」。
- 注：本项目路径含零宽字符，之前几次「编辑丢失/assert 失败」中有一类实为搜索串写错（模板里是 `app.js') }}?v=0.8.7`，用 '?v=' 定位即可），排查时先 grep 实际文本再替换。

## v0.10.1 视频改为手机端录制压缩（13:00 后）
- 用户反馈：视频不会上传；要求在手机上压缩、参数后台设置；并报告百度云出现大量空目录（"源目录后加时间"）。
- 空目录问题=v0.10 已修复的百度 mkdir 同名转存（12:50 修 mkdir_p 改先查再建 + 清理历史空副本），已向用户确认并出示证据。
- 架构调整（C09 修订）：WebView 无法转码已有视频文件 → 改为 **MediaRecorder 边录边压**为主路径：
  · 「🎬 视频」→ 菜单：①录制（推荐）②选择已有视频（服务器 ffmpeg 压缩兜底）
  · 录制参数从后台 settings 下发（/api/video/params）：720p/2500kbps/30fps/最长60s/上限300MB
  · 上传带 precompressed=1 → 服务器跳过 ffmpeg 直接推七牛+百度；输出容器按 MediaRecorder.mimeType 定 ext（mp4/webm）
  · 无声录制：AndroidManifest 有 CAMERA 但无 RECORD_AUDIO → 有声需重打包 APK（后续项）
- 测试：playwright + 假摄像头（--use-fake-device-for-media-stream，executablePath 指向 ms-playwright/chromium-1208）实测录制 5s → 166KB → /api/video 200 → 百度 YJ-515 目录落盘 baidu_ok（服务器未二次压缩，precompressed 生效）。测试文件已清理。
- 坑：录制遮罩 `.rec-mask video{flex:1}` 无 min-height:0 → 视频不收缩把底部按钮挤出视口（playwright 报 element is outside of the viewport）→ 改 flex:1 1 auto + min-height:0 + 工具条 flex:0 0 auto。
- 部署 v=0.10.1，线上 /api/video/params 返回正常。

## v0.11 视频只存本地 + 停止按钮醒目（13:13 后）
- 用户：停止拍摄的地方不明显；视频不要上传，只保留本地。
- 改动：①「🎬 视频」去掉来源菜单与全部上传，点击直接录制；②停止按钮改大红胶囊（230×56，白色方块图标 + "停止并保存到手机" + 呼吸光环动画），顶部条显示大号计时/参数/取消；③停止后本地保存链 saveVideo（Movies 相册，需新版 APK）→ saveFile（下载目录）→ 浏览器下载，并记本机路径提示；④后端 /api/video 保留但前端不再调用（休眠），C09 反转为"只存本地不上传"。
- Android 插件（Java）已加 saveVideo（MediaStore.Video + Movies/{subdir}）并扩展 saveFile 的 mp4/webm/mov mime —— **需重打包 APK 才生效**；当前落下载目录。
- 验证（playwright 假摄像头）：点按钮即进录制 → 3s → 停止 → 保存为 六标_..._视频.mp4（0.2MB），**无 /api/video POST**，本机记录 k=video；停止按钮实测 230×56。
- 部署 v=0.11。

## v0.11.1 原生准备：视频与照片同目录 + 录音权限（13:36 后）
- 用户：重打包后视频能否放图片同目录（路径一致、文件名加视频）？并提供标准化/方便的权限（录音）。
- 结论：可以。MediaStore.Video + RELATIVE_PATH=Pictures/{subdir} 即可与照片同目录（相册里同文件夹）；已把 saveVideo 默认目录改为 Environment.DIRECTORY_PICTURES（base 参数可覆盖），前端提示路径改为 `Pictures/{subdir}/{fname}`。
- 录音关键发现：**Capacitor 6 的 BridgeWebChromeClient 已内置 WebView 权限桥**（VIDEO_CAPTURE→CAMERA、AUDIO_CAPTURE→RECORD_AUDIO+MODIFY_AUDIO_SETTINGS，自动向系统申请并 grant）→ 只需 Manifest 声明权限，getUserMedia 即可带声音；无需自写 onPermissionRequest。
- 标准化权限：插件新增 @Permission(alias="microphone") + `ensureMedia()`（一次申请相机+麦克风，resolvePermissions 返回 {camera,microphone,granted}），前端录制前调用；相机拒→中止并跳设置，麦克风拒→静默降级无声（hint 显示"有声/无声"）。
- 另：saveFile 扩展 mp4/webm/mov mime（原 octet-stream）。新增 doc/008-APK重打包指引.md：CI 自动出包（push android/** 即触发 android-build.yml）+ secrets 签名版 + 本机构建 + 7 项验证清单；注意 debug 包与正式包签名不同→需先卸旧版（或走签名版覆盖升级）。
- 前端 v=0.11.1 已部署；android/** 已推送触发 CI。

## APK 分发（13:54 后）
- 用户找不到 APK → 用 gh run download 取 CI 产物（4.5MB app-debug.apk），校验 zip/dex/Manifest 后放到 static/，并提供两种下载口：
  · 长直链 https://forest.bibook.top/supervision/static/supervision-v0.11.1-debug.apk
  · **新增 Flask 短链 /apk**（main.py apk_latest()：取 static/supervision-*.apk 中版本号最大者，attachment + apk mime），便于手机输入与二维码。
- 生成二维码 /tmp/apk_qr.png（指向短链）。注意：APK 直链是**公开可下载**（静态目录不受网关登录保护），已告知用户可随时下线。
- 后续更新 APK 流程：CI 打包 → gh run download → scp 到 apps/supervision/static/ → /apk 自动指向最新。

## v0.12 视频格式兼容性（14:04 后）
- 用户反馈"显示无法播放视频，视频是否可以 mp4"。
- 三类可能原因：①录到 WebM 容器（部分国产 ROM 相册不支持 VP8/VP9）②旧 APK 的 saveFile 把 mime 写成 octet-stream（播放器拒播；新 APK 已改）③数据不完整。
- 本地实测（Chrome for Testing + 假摄像头）：录出的文件头 `00000024 66747970 69736f6d` = ftyp/isom → **真 MP4**；recorder.mimeType=video/mp4;codecs=avc1.42E01E。
- 改动 v0.12：①录制容器只认 MP4/H.264（MP4_MIMES 优先 avc1.42E01E，避免落到 HEVC/AV1）②`mp4RecSupported()` 为假时**自动改走系统相机**（file input capture=camcorder → 硬件 H.264 必播），录完用 saveVideo 按同一命名另存（saveExternalVideo）③录制 hint 显示真实容器（MP4/WebM），若最终是 WebM 则红字提示改用系统相机 ④C09 补"容器只认 MP4/H.264"。
- 排查提示固化：判断视频能否播放先看**文件头**（ftyp=MP4 / 1a45dfa3=WebM），再看 MediaStore 的 mime（octet-stream 会拒播）。

## v0.13.1 默认改用系统相机录制（14:06 后）
- 用户反馈：华为 P40 Pro+ 上录出来是 WebM（相册无法播放）→ 确认该机型 WebView 的 MediaRecorder 只能出 WebM。
- 决策：新增 settings `video_rec_mode`，**默认 system**（系统相机录制 → 硬件 H.264/MP4，任何相册必播）；`inapp` 保留（省流量，部分机型 WebM）。
- 前端：按 rec_mode 决策；`inapp` 下若实际产物是 webm → 红字提示 + 自动引导系统相机重录（并保存 webm 副本不丢）；录制参数每次点击实时拉取（修复"后台改设置前端缓存不生效"）；toast 文案去重。
- 验证（playwright）：system 模式点按钮 → 直接调系统相机（#videoInput click=1，无录制遮罩，toast 简洁）；inapp 模式 → 录制遮罩出现、不调相机；线上 params 返回 rec_mode=system。
- 部署 v=0.13.1（前端改动即时生效，无需新 APK）；doc/008 增「录制方式」对比表；STATE 更新。
- 经验：华为/HarmonyOS 机型录制兼容性以"系统相机"为最稳路径；页面录制仅适合作省流量备选。

## v0.13.2 修复"点录像确认后要求登录"+ 原生录像（14:16 后）
- 用户反馈：点录像 → 系统相机录制 → 点确认回来，App 弹出登录页。
- 根因：系统相机是独立 Activity，会顶掉/重启 WebView；而 Flask 的 session cookie 是**会话级**（无过期时间），WebView 重启即丢 → 页面重载 → boot() 拿不到会话 → 显示登录页。
- 修复①：`app.permanent_session_lifetime = timedelta(days=30)` + 登录时 `session.permanent = True`（生产 prefix 下加 SESSION_COOKIE_SECURE=Lax）；线上验证 cookie 过期时间为 30 天后（1791872269）。
- 修复②（治本）：插件新增 **recordVideo({name,subdir,maxSeconds,quality})** —— 用 startActivityForResult 调系统相机录像，@ActivityCallback 里**流式复制**（256KB 缓冲，不整块读内存）到 Pictures/{subdir}/，resolve 真实路径；不再走 WebView 的 <input capture> 文件回传，因此页面重载也不丢文件、容器为相机原生 MP4/H.264。前端 useSystemCamera 优先调用它，失败/旧 APK 再回退文件选择。
- 前端另加：401 时明确 toast「登录已过期，请重新登录」；新增 tsStamp() 复用时间戳。
- 部署 v=0.13.2（会话修复即时生效）；android/** 已推送 → CI 自动出新 APK。
- 新 APK v0.13.2（4,503,218B，commit 240affb）已发布到 static/，短链 /supervision/apk 自动指向它；dex 中确认含 recordVideo/ensureMedia/saveVideo 符号。
- APK 发布流程（可复用）：gh run list → gh run watch → gh run download → 校验 zip/dex → scp 到 apps/supervision/static/supervision-vX.Y.Z-debug.apk（旧包保留可回滚，/apk 取版本号最大者）。

## v0.14 原生录像压缩（14:22 后）
- 用户：原生插件录像能否像拍照那样压缩 + 同目录同命名？→ 目录/命名本来就一致；压缩新增。
- 实现：androidx.media3:media3-transformer 1.4.1 + media3-effect；Transcoder 流程 = 取源高度 → 计算 scale（≤video_max_height）→ Transformer(videoMimeType=H264, audioMimeType=AAC, EncoderFactory 设码率) → 临时文件 → 流式复制进 Pictures/{subdir}/{name}；失败回退原片；notifyListeners('videoStage') 提示"压缩中/压缩失败"；EXTRA_VIDEO_QUALITY=0 录制。
- **API 适配踩坑（CI 编译两轮失败）**：①`DefaultEncoderFactory.Builder#setRequestedMaxVideoBitrate` 在 1.4.1 **不存在** → 正确是 `setRequestedVideoEncoderSettings(new VideoEncoderSettings.Builder().setBitrate(bps).build())`；②`Effects` 构造是 `(audioProcessors, videoEffects)`，参数顺序反了会报 "inference variable T has incompatible bounds"（不要用 guava ImmutableList，普通 List 即可）。定位方法：从 dl.google.com maven 下 aar → 解 classes.jar → 用 python 正则抓 class 内字符串/描述符核对方法名与参数顺序（无需 JDK/javap）。
- 结果：CI success，APK v0.14（7,276,092B，dex 含 recordVideo/transcodeAndSave/VideoEncoderSettings/ScaleAndRotateTransformation/videoStage）已发布，/apk 指向 v0.14。

## v0.15 长期令牌 + 启动一次性申请权限（14:44 后）
- 用户：视频确认后还是弹登录；能否安装后提前申请所有权限（后台运行，轨迹页用得着）。
- 根因分析：系统相机把 WebView 进程挤掉时，cookie 尚未落盘 → 回来"未登录"。所以 cookie 方案本身不够稳。
- 修复（**不依赖 cookie**）：服务端加 HMAC 令牌（180 天，base64(user|exp|sig)）——登录返回 token，`auth_user()` 接受 session / `X-Sup-Token` / `?token=`；新增 `/api/me` 供页面重载后恢复身份；前端 token 存 localStorage（写入即持久）、`api()` 自动带头、导出与下载链接带 `?token=`；退出清 token。
- 验证：①curl 纯令牌访问 200、无令牌/篡改 401、导出带 token 200；②playwright 清空 cookie 后重载 → **不弹登录**、列表正常、用户名自动恢复（关键证明）。
- 权限：登录进入后一次性申请「相机+麦克风（ensureMedia）+ 定位（request location）」，之后再无中途弹窗；无需新 APK（用的是 v0.13.2+ 已有方法，旧包自动跳过缺失方法）。
- 部署 v=0.15，线上令牌接口验证通过。

## v0.14.1 启动图标重做（14:46 后）
- 用户：app logo 变了，应该是绿色盾牌松树勾那个。
- 排查：①APK 内图标与仓库 md5 一致 → 图源没错；②真因是**自适应图标安全区**——素材 generated-images/Flat_modern_Android_app_icon_*.png 是 1024 全幅绿底（右下角还有"AI生成"水印），旧前景只占画布 65%（emblem 只有 49%）且叠在相近色背景上 → 看起来"绿方块里一个小盾牌"；素材 emblem 本身占高 76%，超过安全区 64%，直接放大又会被圆形遮罩切掉盾牌尖角。
- 解决（脚本固化 tools/make_app_icons.py，可重复执行）：去水印（背景模型填充+模糊过渡）→ 素材缩到画布 84%（emblem≈62%，安全区内）+ **边缘延展**填满画布消内嵌接缝 → 传统方形/圆形图标同款 → 自适应背景取素材四角绿 #215C33。
- 验证：本地合成圆形/圆角/方形三种遮罩预览，盾牌完整不裁切、无接缝；CI 编译 success。
- APK v0.14.1（7,303,892B）已发布，/supervision/apk 指向它。
- 顺带：确认 release v0.8.0 包开启了资源混淆（res 文件名被改写），无法按名读取图标 → 排查图标用 debug 包即可。
