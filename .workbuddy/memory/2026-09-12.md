# 2026-09-12

## v0.8 拍照本地化与验收联动（doc/006）
- 相片不落服务器（新约束 C07）：删后端 photos 路由；App 存系统相册（失败回退下载）、浏览器直接下载；前端 localStorage 按工作簿记拍摄文件名提示（拍摄记录弹层），无预览。
- 导出按上传模板回填（新约束 C08）：上传留 data/workbooks/<id>/source.xlsx；_export_workbook() 清数据行回填，前端 /api/workbooks/<id>/export + 后台 download 同链路。
- 验收联动 syncAcceptCols：选结果填验收人(登录用户)+验收日期；选空清 验收人/验收日期/验收备注；setValueFromCoords 后 DOM 强刷双保险。
- 变更日志：accept_logs 表（验收4字段 diff，api_save_row 内写），后台「验收变更日志」表。
- 压缩 1600px/0.85；{{拍照人}} 占位符（RESERVED_PH）；app.js?v=0.8 缓存熔断（「验收人不更新」根因=WebView 旧缓存，非逻辑 bug）。
- 真机端到端全通过：导出 1174 行格式保留、日志 5 条、3200×2400→1600×1200、拍摄提示列表。
- 待办：./deploy.sh 上线 v0.8；旧 wb2 无 source 需重新上传才能导出。
- 环境：本机跑本项目用 /usr/bin/python3（managed 3.13 无 flask/openpyxl）；8720 常驻旧进程，测试用 create_app().run(port=8721)。

## Keystore 与备案（v0.8 后追加）
- 已生成 android/supervision-release.jks（alias=supervision，RSA2048，有效期 10950 天），密码在 android/keystore.properties（均 git-ignore）；备份在 ~/Documents/bibook-keystore-backup/hqz-supervision/。
- 证书指纹（APP备案用）存 android/keystore-info.txt；SHA1 54:C1:D0:0A:D0:B3:12:A9:EC:C3:7D:6A:29:34:38:67:C0:D9:3C:AC。
- android/app/build.gradle 接入 keystore.properties 签名（无凭据时回退 debug 签名可出包）；新增 .github/workflows/android-release.yml（tag v* 或手动触发，Secrets 四项）。
- 备案结论：bibook.top 已 ICP 备案只覆盖网站；App 需另做「APP备案」（同一主体，阿里云等接入商入口，需包名 top.bibook.supervision + 签名公钥指纹）；直接发 APK 内部使用不上商店则实际不卡。

## App 更名 监理验收 + 图标
- appName 全链路改为「监理验收」：capacitor.config.json、android strings.xml(app_name/title_activity_main)、www/index.html、android assets/public、templates/index.html、static/app.js toast、package.json。appId 不变 top.bibook.supervision。
- 图标：ImageGen 生成（绿盾+金勾+松树，1024px，裁底部8%去 AI 水印），PIL 批量出 mipmap-{mdpi..xxxhdpi} ic_launcher(圆角)/round(圆)/foreground(66% 安全区)，背景色 XML 同步 #225D34；mipmap-anydpi-v26 引用未动。
- 注意：下次 gradle 构建后 android/app/src/main/assets/public 会被 cap sync 刷新，以 www/ 为源。

## 推送打包（v0.8.0）
- 已推送 main（60a4873 v0.8 全量 → 3f7a2ad CI 合并 → eb8f97c gradle 回退）+ tag v0.8.0，Actions 全绿。
- CI 收敛为单个 release.yml（tag v* 触发：构建→发布 GitHub Releases，名称「监理验收」）；删了重复的 android-release.yml；android-build.yml（main push 触发 debug）保留，其 60a4873 一次 Gradle 失败为偶发（同 commit 其他两个工作流均成功）。
- v0.8.0 Release 现有 2 资产：app-debug.apk（可装）+ app-release-unsigned.apk（不可装，Secrets 未配置所致）；Release 名称仍是旧「监督验收」（首次运行创建，action-gh-release 未改名）。
- 待办：gh auth login 后跑 set_gh_secrets.sh 配 4 个 Secrets → 删 tag/Release 重打 → 得签名 app-release.apk。gradle 已修：无 keystore.properties 回退 debug 签名（产物始终可安装）。

## 正式签名包完成
- gh auth login 后跑 set_gh_secrets.sh 配好 4 个 Secrets（老版 gh 无 --body-file，改 stdin 传 KEYSTORE_BASE64）。
- 两次失败教训：① 无 Secrets 时出 unsigned 包不可安装 → gradle 加 debug 回退；② validateSigningRelease 失败根因=gradle file() 相对 app 模块解析，jks 在 android/ 根 → 改 rootProject.file()（c8b401d）。
- v0.8.0 Release 终态：名称「监理验收 v0.8.0」，唯一资产 app-release.apk 3.5MB，keytool 验签 SHA1=54:C1:D0:...:3C:AC 与本地 keystore 一致。
- 以后发版流程：push main → git tag vX.Y.Z && git push origin vX.Y.Z → Release 页自动出签名包。

## v0.8.1（09:47 后）
- 用户反馈：点拍摄图片预览死机 → 根因：线上仍跑 v0.7.x 相册（img 缩略图+大图预览撑爆 WebView）；v0.8 已砍预览但未部署。
- 改动：拍摄记录改显完整文件 path（App: Pictures/{目录}/{文件名}.jpg；浏览器: 文件名.jpg），旧记录仍只存文件名；.shot-item 长路径 word-break 换行；缓存熔断 v=0.8.1。
- ./deploy.sh 部署上线，curl 验证 app.js?v=0.8.1 已生效；STATE.md 更新；push GitHub（c8b401d..b5ace2e）。

## v0.8.2（20:05 后）
- 用户报：联动提示已填验收人但值未变；导出有验收时间无验收人。
- 根因（浏览器自动化复现确认）：登录是 fetch 静默完成 preventDefault 不刷新页面 → #whoami data-user 恒空 → syncAcceptCols setCol('验收人','') 空对空跳过，toast 仍报。线上 accept_logs 证实验收人从未变化。
- 修复：/api/login 返回 user + 前端回写 data-user；toast 只报真实填入字段；验收结果 label ★绿底突出（td[data-x="0"]，注意 td[0] 是 jss 行号列）；列表页加工作簿⬇导出；缓存 v=0.8.2。
- 附带发现：本地 8720 有残留旧版 dev server（内存缓存旧模板）干扰排查——改代码验证前先 pkill main.py 重启。
- 端到端验证：选合格 → 验收人=雷华雄/日期/结果全落库 + accept_logs 三条；已部署上线并 curl 验证。

## doc/007 相片云同步设计（22:02 后）
- 用户需求：拍照照片参考 trans_v2 同步百度云同名目录；插件式；后台配置压缩参数/同步平台/同步参数，启动开关才同步；trans_v2 作默认。
- 评审：trans_v2 baidu_pan.py 可直接移植（OAuth/token 续期/分片上传现成）；手机直连百度不可行（WebView 壳改造成本高）→ 服务器中转。
- 用户自提 COS 方案：手机→服务器→腾讯 COS 暂存（状态标记）→百度网盘，评审结论可行且推荐（手机只依赖第一跳，与野外网络解耦）。
- 状态机 received→cos_ok→baidu_ok；C07 修订为"不长期存储"；压缩参数走参数 sheet、同步配置走后台 settings（业务跟模板走、基础设施跟后台走）。
- 产出 doc/007-相片云同步设计.md（设计评审稿未实现）；开放问题：COS bucket 是否已有、百度 redirect_uri、浏览器端是否同走中转。

## 双腿实测（23:13 后）
- 用户确认：浏览器端不拍照；百度授权=直接复制 trans_v2 数据；中转用已有七牛 bucket zz-1（弃 COS）。
- 测试结果：百度腿一次通过（token 剩 12.6 天，refresh 自动续期；上传 /apps/book_translator/_sync_test/ 成功+list 验证）；七牛腿初报 612/BadToken——根因：config.json 的七牛 AK 是占位符（YOUR_ 前缀），真凭证只在 config.local.json.bak_github/.bak/.bak_kimi/.bak_gateway；用真凭证后 zz-1 上传成功（buckets=['sux789','zz-1']）。
- ⚠️ trans_v2 config.local.json 丢失 cloud 段（9/10 清理所致），生产 QiniuStorage 现抛 CLOUD_CONFIG_MISSING——只报告未代改（bak_gateway 命名暗示可能有意迁移）。
- 坑：qiniu SDK 7.18 ResponseInfo 的 resp_body/text_body 是属性不是方法；SDK 自动区域探测接口偶发 612。
- doc/007 已定稿（qiniu_ok 状态、token 导入免授权页、M1-M3 里程碑），push ee96647。

## v0.9 相片云同步实现上线（23:26 后）
- 按用户"开始写代码，后端配置，使用当前默认参数"实现 doc/007 全链路：sync_cloud.py（纯标准库：七牛 v4/query 区域查询 + 签名直传/删除 + 百度 xpan precreate/superfile2/create + token 续期）、main.py 建表 settings/photo_sync + /api/photo + /api/sync/enabled + admin sync API、前端拍照中转 + 后台同步设置卡、param_parser 新增压缩最长边/压缩质量、deploy.sh 加 sync_cloud.py。
- 凭证：本地与服务器 settings 表均写入 trans_v2 真实 AK/SK + 百度凭证 + token（enabled=1）；真实凭证绝不进代码/git。
- 调试坑（按序）：①七牛回退域名 z2 笔误 qiniu.com→qiniup.com；②uc v4/buckets/region 404 → 正确免签名端点 /v4/query?ak=&bucket=（zz-1=z2 华南，io=iovip-z2）；③七牛 io 源站域名全部 404 no such domain → 回读改 data/pending 本地缓冲（百度成功即删），七牛副本作备份；④superfile2 打错到 xpan/file 端点 → 403 31064，必须走 d.pcs.baidu.com/rest/2.0/pcs/superfile2；⑤百度 mkdir /apps 层级 errno=102 → 跳过 app_dir 固定前缀只建子层级；⑥百度对已存在目录偶发 _时间戳 副本目录（无害）。
- 网盘目录：/apps/book_translator/supervision/{参数目录}/{文件名}.jpg（app_key 决定固定目录，supervision 子目录隔离）。
- 验证：本地 curl 照片→baidu_ok；线上（forest.bibook.top/supervision）curl 照片→baidu_ok；测试产物双云已清理。
- 本会话 Edit 工具对含零宽字符路径（hqz-supervision‌）偶发"成功但未落盘"：import sync_cloud、pending 写入、BaiduPan app_dir 三处中招，重要编辑后必须 grep 验证。
