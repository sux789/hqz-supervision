# 2026-09-11

## hqz-supervision 骨架版 v0.1
- 参数 sheet 标准化：副本中幼林抚育.xlsx → 表头 key|value|类型|默认值|说明|示例，8 键（新增 相片文件名），统一分隔符（; 多值 / | 子字段 / / 目录 / _ 文件名段 / {{}} 占位符），修笔误 小班号;select。原文件备份 /Users/sux/Documents/副本中幼林抚育.backup-20260911.xlsx。
- 调研：hqz-survey 拍照/轨迹全部硬编码 app.js（buildPhotoName L3033、photoSaveSubdir L3005、PHOTO_SAVE_DIR L2984），参数 sheet 在 hqz-survey 中完全未被消费——是本项目新增契约。
- 骨架：project-template 整目录复制至 hqz-supervision‌（git init，提交 f6fa196）；AI_INSTRUCTIONS 填业务/输入/输出；CONSTRAINTS 注册 C01（参数驱动禁硬编码）/C02（参数语法）；资产拷贝 data/{中幼林抚育-网格填表模板.xlsx, tpl-base.xlsx, tpl-samples.xlsx}。
- 设计文档：doc/001-参数sheet配置规范.md、doc/002-拍照轨迹功能迁移设计.md（决策 D1~D6、迁移批次 B1~B4）。
- 注意：模板自带 inspect.py 与 stdlib 冲突，在该目录跑 python 需 cd /tmp 或改名。

## 搜索口径补充（16:17）
- search 控件定为前端本地搜索（不发服务端请求）：数据全量已在前端，hqz-survey 原服务端 api/subcompartments/search 已删（app.js L565 注释可证）；弱网/离线可用。select=选项≤50，search=选项过多。将来十万行级再扩展 字段|search|remote 修饰位。
- 已同步三处：xlsx 参数 sheet E9 说明、doc/001 决策段、data/ 模板副本。教训：写单元格前先确认行号（E8 是功能行，E9 才是搜索选项，曾误写后已修复）。

## 定位修正 v0.2（16:40）
- 用户修正定位：hqz-supervision 是通用 Excel 网格填表工具，非调查 App 复制品——无样地/GDB/打卡业务，行为全由参数 sheet 驱动；支持上传多个 xlsx 列表选择进入（IndexedDB）；先本地预览不部署、不影响 hqz-survey 线上；项目不重复（独立 appId/路径）。
- 技术复用确认：hqz-survey 前端网格是 jspreadsheet-ce@4 + jsuites（base.html L26-27，app.js L1327），后端 openpyxl；新项目纯前端用 jspreadsheet-ce + SheetJS 解析上传 + IndexedDB。
- 文档：doc/003-项目定位修正与通用网格填表设计.md（架构/选型/页面流/批次修订）；CONSTRAINTS +C03（不影响线上）/C04（通用性）；AI_INSTRUCTIONS/STATE 同步。

## v0.3 B1 本地预览版（17:00-17:15）
- 新增代码：main.py（Flask:127.0.0.1:8720，登录/上传/列表/读取/保存/admin后台/轨迹接口）、param_parser.py（C02 校验：未知key/全角分隔符/占位符列表头不匹配→带行号报错）、templates/{index,admin}.html、static/{app,admin,style}。
- 账号：雷华雄/lhx123（admin，sha256，SQLite users 启动 seed，C05）。后台：模板管理+下载Excel(数据+参数双sheet重生成)+轨迹下载(zip)。
- 关键修正：①参数表 相片文件名 {{分类}}→{{标段}}（分类非真实表头，C02会报错）②openpyxl read_only 维度误报9列，iter_rows 需 max_col=64 强制扩列 ③inspect.py 重命名 inspect_tpl.py 解决 stdlib 遮蔽 ④强度列数值0.18规范化为文本百分比。
- 冒烟全过：登录/上传1173行/参数解析/保存/下载Excel(13表头+参数10行)/C02报错带行号(全角、占位符)/轨迹上传列表zip/未登录401。
- 反转：doc/003 纯前端+SheetJS → 轻后端 Flask+openpyxl 服务端解析（因后台需求）。
