# 2026-09-11

## hqz-supervision 骨架版 v0.1
- 参数 sheet 标准化：副本中幼林抚育.xlsx → 表头 key|value|类型|默认值|说明|示例，8 键（新增 相片文件名），统一分隔符（; 多值 / | 子字段 / / 目录 / _ 文件名段 / {{}} 占位符），修笔误 小班号;select。原文件备份 /Users/sux/Documents/副本中幼林抚育.backup-20260911.xlsx。
- 调研：hqz-survey 拍照/轨迹全部硬编码 app.js（buildPhotoName L3033、photoSaveSubdir L3005、PHOTO_SAVE_DIR L2984），参数 sheet 在 hqz-survey 中完全未被消费——是本项目新增契约。
- 骨架：project-template 整目录复制至 hqz-supervision‌（git init，提交 f6fa196）；AI_INSTRUCTIONS 填业务/输入/输出；CONSTRAINTS 注册 C01（参数驱动禁硬编码）/C02（参数语法）；资产拷贝 data/{中幼林抚育-网格填表模板.xlsx, tpl-base.xlsx, tpl-samples.xlsx}。
- 设计文档：doc/001-参数sheet配置规范.md、doc/002-拍照轨迹功能迁移设计.md（决策 D1~D6、迁移批次 B1~B4）。
- 注意：模板自带 inspect.py 与 stdlib 冲突，在该目录跑 python 需 cd /tmp 或改名。

## 搜索口径补充（16:17）
- search 控件定为前端本地搜索（不发服务端请求）：数据全量已在前端，hqz-survey 原服务端 api/subcompartments/search 已删（app.js L565 注释可证）；弱网/离线可用。select=选项≤50，search=选项过多。将来十万行级再扩展 字段|search|remote 修饰位。
- 已同步三处：xlsx 参数 sheet E9 说明、doc/001 决策段、data/ 模板副本。教训：写单元格前先确认行号（E8 是功能行，E9 才是搜索选项，曾误写后已修复）。

## 定位修正 v0.2（16:40）
- 用户修正定位：hqz-supervision 是通用 Excel 网格填表工具，非调查 App 复制品——无样地/GDB/打卡业务，行为全由参数 sheet 驱动；支持上传多个 xlsx 列表选择进入（IndexedDB）；先本地预览不部署、不影响 hqz-survey 线上；项目不重复（独立 appId/路径）。
- 技术复用确认：hqz-survey 前端网格是 jspreadsheet-ce@4 + jsuites（base.html L26-27，app.js L1327），后端 openpyxl；新项目纯前端用 jspreadsheet-ce + SheetJS 解析上传 + IndexedDB。
- 文档：doc/003-项目定位修正与通用网格填表设计.md（架构/选型/页面流/批次修订）；CONSTRAINTS +C03（不影响线上）/C04（通用性）；AI_INSTRUCTIONS/STATE 同步。

## v0.3 B1 本地预览版（17:00-17:15）
- 新增代码：main.py（Flask:127.0.0.1:8720，登录/上传/列表/读取/保存/admin后台/轨迹接口）、param_parser.py（C02 校验：未知key/全角分隔符/占位符列表头不匹配→带行号报错）、templates/{index,admin}.html、static/{app,admin,style}。
- 账号：雷华雄/lhx123（admin，sha256，SQLite users 启动 seed，C05）。后台：模板管理+下载Excel(数据+参数双sheet重生成)+轨迹下载(zip)。
- 关键修正：①参数表 相片文件名 {{分类}}→{{标段}}（分类非真实表头，C02会报错）②openpyxl read_only 维度误报9列，iter_rows 需 max_col=64 强制扩列 ③inspect.py 重命名 inspect_tpl.py 解决 stdlib 遮蔽 ④强度列数值0.18规范化为文本百分比。
- 冒烟全过：登录/上传1173行/参数解析/保存/下载Excel(13表头+参数10行)/C02报错带行号(全角、占位符)/轨迹上传列表zip/未登录401。
- 反转：doc/003 纯前端+SheetJS → 轻后端 Flask+openpyxl 服务端解析（因后台需求）。

## Android 打包壳（17:25）
- 复用 hqz-survey 远程 URL 壳模式：Capacitor6 + www 壳页 + WebView 加载 server.url。appId top.bibook.supervision / appName 监督验收 / URL 占位 https://forest.bibook.top/supervision。
- npx cap add android 已生成完整 android/ 工程并提交（本机无 Android SDK，构建需在装 SDK 的机器跑 ./gradlew assembleDebug）；本地联调改 10.0.2.2:8720 + cleartext（doc/005）。
- gitignore 补 node_modules/android 构建产物。
- GitHub：仓库 sux789/hqz-supervision 不存在、gh 未登录；SSH key 可用（推送没问题），建仓需用户操作或 token。

## GitHub 推送 + B2 拍照（17:30-18:10）
- GitHub：sux789/hqz-supervision 建仓完成，main 已推送（v0.1~v0.3.1 共 4 提交，SSH key 认证）。remote origin 已配置。
- Android 壳（v0.3.1）：Capacitor6 远程 URL 模式，appId top.bibook.supervision，android/ 工程已提交；本机无 SDK，构建在装 SDK 的机器跑 gradlew。
- B2 拍照完成（v0.4）：前端 网格点选行→📷拍照→createImageBitmap 水印（日期固定行/坐标best-effort 2.5s/备注模板22字折行，黑字白边 JPEG0.92）→模板渲染文件名+目录→上传；相册弹层+zip。后端 _safe_segments 清洗（穿越400）、重名 _2 后缀、photos/{subdir}/ 存储。admin 加"下载相片"。
- 冒烟 7 项全过：上传(参数化目录)/重名后缀/路径穿越400/列表/取图200 image/jpeg/zip 内容正确/未登录401。测试相片已清理。
- 技术点：jspreadsheet onselection 跟踪 selRow 作拍照上下文；toLocaleDateString('sv-SE') 得 YYYY-MM-DD；前端 sanitizeSeg 与后端 _safe_segments 同规则。

## GitHub Actions 云端打包（18:17-18:22）
- .github/workflows/android-build.yml：push 触及 android/** www/** cap 配置自动打包 + workflow_dispatch 手动；npm install → cap sync → gradlew assembleDebug → 上传工件「监督验收-debug-apk」。
- 首跑 #1 即成功（约2分钟），APK 3.6MB，保留30天；run id 34588503452。
- 关键点：android/.gitignore（Capacitor 自带）忽略 assets/capacitor.config.json 与 capacitor-cordova-android-plugins/，构建前必须 cap sync 再生成；capacitor.settings.gradle 引用 ../node_modules，CI 必须 npm install。
- 监控用公开 API（仓库 public，无需 gh 登录）：/actions/runs?per_page=1。
- doc/005 增补 §3.1 云端打包说明。

## B3 轨迹完成（19:12-19:20）
- 前端：btnTrack（功能含轨迹才显示），watchPosition enableHighAccuracy 采集 {lat,lng,ele,t}，实时点数显示+红色脉冲；停止→buildGpx（GPX1.1 trkpt/ele/time，XML 转义）→文件名 轨迹_{小班号}_{ts}.gpx→/api/track 上传；<2点提示不上传。recording 样式 recPulse。
- 冒烟：模拟 GPX 上传（自动加时间戳前缀）/admin 列表/zip/单文件 200/未登录 401 全过；测试文件已清理。node --check 语法 OK。
- v0.5：doc/002 B3 标记完成、STATE 更新。前端改动不触发 APK CI（paths 不含 static/**，符合预期）。

## 部署上线（19:42-20:00）
- https://forest.bibook.top/supervision 上线成功（gateway 挂载）。生产冒烟：登录/上传1173行/静态/admin/未登录401 全过。
- 部署机制（同 hqz-survey gateway 模型）：appspec（deploy/supervision.appspec）→ scp 到 gateway/apps_root → apps 表 INSERT OR IGNORE（forest_web.db）→ systemctl --user restart gateway.service。写进 ./deploy.sh。
- 关键坑：DispatcherMiddleware 已设 SCRIPT_NAME 剥前缀 → 路由不得挂 url_prefix（否则双重前缀404）；static 用默认 /static 规则即可命中；前端用 window.BASE=request.script_root 保证 fetch/href 前缀安全；factory 由 gateway 带 prefix 参数调用（main:create_app）。
- gateway 环境：shared_venv（有 flask+openpyxl，零新依赖）；appspec 的 static_url_path 字段仅为声明（gateway 不消费）。
- 排查手段：shared_venv python 手动 load factory 验证；直连 127.0.0.1:8090 排除 nginx；对拍 /survey 308。

## 管理端参数摘要 + 口径确认（20:05-20:15）
- 用户确认：①管理端就用 /supervision/admin（不加新路由）②没有打卡，只有拍照+轨迹（C04 的"无打卡业务"维持，参数表功能键的'打卡'可选项暂保留定义但未启用）③分工保持：管理端管模板，拍照轨迹在用户端网格页。
- 轨迹/拍照位置：用户端网格页工具栏 📷拍照/相册/◎轨迹，由参数「功能」控制显隐（app.js L181-184），需先打开工作簿。
- 新增：admin_list 返回参数摘要（可编辑列/隐藏列/功能/验收结果选项，split_list 归一化），admin 表格加「可编辑项/功能」列。坑：config 里列表类 key 存原始分号字符串（param_parser cfg[key]=value 不 split），前端直接 .join 会炸。
- 已部署上线验证通过，提交推送。
