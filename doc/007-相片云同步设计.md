# 007 - 相片云同步设计（服务器接收 → 七牛 zz-1 暂存 → 百度网盘）

> 状态：**设计定稿，双腿实测通过（2026-09-12），未实现**。用户原话驱动。
>
> 2026-09-12 深夜确认：① 浏览器端不拍照（/api/photo 仅 App 端）② 百度授权直接复用 trans_v2 数据（token + 凭证成套复制，免授权页）③ 中转平台改用**已有七牛云 bucket zz-1**（弃 COS）。

## 0. 双腿实测结论（2026-09-12 23:21，本机真云验证）

| 云 | 结果 | 明细 |
|---|---|---|
| 七牛 zz-1 | ✅ 上传成功 | 真实 AK/SK 下 buckets=['sux789','zz-1']，put_data 直传成功；SDK 自动区域探测正常 |
| 百度网盘 | ✅ 上传成功 | 复用 .baidu_pan_token.json（剩 12.6 天有效，refresh_token 自动续期），上传 /apps/book_translator/_sync_test/test_*.jpg 成功 + list_dir 验证通过 |

测试产物：七牛/百度 `_sync_test/` 前缀文件，可随时清理。测试脚本思路：模拟 64KB JPEG 魔数字节 → put_data / BaiduPan.upload_file(overwrite=True)。

⚠️ **重要发现（trans_v2 侧）**：`config.local.json` 当前**丢失 cloud 凭证段**（2026-09-10 备份时被清），真实七牛 AK/SK 只存在于 `config.local.json.bak_github` / `.bak` / `.bak_kimi` / `.bak_gateway`——trans_v2 生产七牛上传现会抛 CLOUD_CONFIG_MISSING。若 trans_v2 七牛功能还要用，需从备份恢复 cloud 段（AK 前缀 E9c3WYaFbh...）。另：百度凭证 config.json 与 config.local.json 不一致（local 有 override），supervision 复制时**凭证与 token 必须成套同源**（app_key/secret 必须与签发 token 的应用一致）。

## 1. 需求背景（用户原话）

> 相片同步，参考 /Users/sux/Desktop/trans_v2 同步到百度云的 app 同名目录下，是否可行？插件方式，比如后台设置压缩参数，设置同步平台，这样是否可行？如果可行，后台就设置同步参数，启动同步参数才同步，百度云参数也在后台配置中，先用 trans_v2 作为默认，评审和优化我的思路。
> （失败策略追问后补充）服务器转推百度失败时：同步到腾讯对象存储，同步成功留下状态标记，再从腾讯存储同步，成功写入百度云，写入百度云成功，再标记，是否可行。

## 2. 可行性评审结论

**结论：可行，且「七牛 zz-1 暂存 + 状态标记」链路比纯中转更稳，推荐采纳（双腿已实测通过）。**

### 2.1 trans_v2 复用评估（已核实）

trans_v2 `cloud/baidu_pan.py` 是完整的百度网盘开放平台客户端，可直接移植：

| 能力 | 状态 | 说明 |
|---|---|---|
| OAuth 授权 | ✅ | get_auth_url + exchange_code，token 持久化 JSON |
| token 自动续期 | ✅ | refresh_token 有效（30 天期），_refresh_token 现成 |
| 分片上传 | ✅ | precreate → superfile2（4MB/片）→ create 三步走 |
| 已存在覆盖 | ✅ | 先删后传，处理了 `_YYYYMMDD` 副本坑 |
| 列目录/分享/配额 | ✅ | 后台状态面板可直接用 |

移植方式：复制进 `sync/baidu_pan.py`，剥离 `infra.debug / infra.error_codes / infra.if_throw` 依赖（约 3 处调用，改为标准 logging/异常）。百度应用凭证复用 trans_v2 的同一套 app_key/secret（授权一次即可）。

### 2.2 链路方案对比

| 方案 | 链路 | 可靠性 | 复杂度 | 磁盘/费用 | 结论 |
|---|---|---|---|---|---|
| A 纯中转 | 手机→服务器→百度 | 低：野外信号差，百度推送失败即丢（手机相册兜底） | 最低 | 零 | 否 |
| B 服务器磁盘队列 | 手机→服务器→本地 pending→百度 | 中：服务器磁盘/重置风险 | 低 | 服务器磁盘（每张 300-500KB） | 备选 |
| **C 七牛 zz-1 暂存（定稿）** | 手机→服务器→**七牛**→百度 | **高：对象存储高可用，百度推送失败可无限重试，断点可续** | 中 | 零新增成本（复用现有账号与 bucket） | **推荐（已实测通过）** |

C 方案的核心优势：**手机端只依赖第一跳**——照片传到服务器/七牛成功即安全，剩下的百度推送由服务器慢慢补，与野外网络状况解耦。七牛账号现成（trans_v2 同款 AK/SK 实测可访问 zz-1），零开通成本；服务器→七牛上行走公网但单张仅 300-500KB，无压力。

### 2.3 风险与对策

| 风险 | 对策 |
|---|---|
| 双云凭证配置（七牛 AK/SK + 百度 app_key/secret） | 全部收口在后台「同步设置」，未配置则开关不可开启；百度侧支持直接粘贴/导入 trans_v2 的 token JSON，免授权页 |
| 百度 API 频率限制 | 推送间隔 1s/张；批量补推走队列限速 |
| 七牛副本清理 | 百度成功后默认**保留 30 天再清**（兜底灾备），后台可改「永久保留/立即删除」 |
| token 失效 | refresh_token 自动续期；续期失败 → 后台标红提示重新授权 |
| 重复上传（重试导致网盘重名副本） | 百度 precreate 幂等：同一 remote_path + md5 秒传/已存在直接返回（trans_v2 已处理） |
| C07 冲突 | 修订为「照片不在服务器**长期**存储」：成功即删暂存，暂存只存在于七牛 zz-1（服务器不留副本） |

## 3. 架构与状态机

```
手机拍照（压缩+水印，现有链路不变）
  ├─ 存系统相册 / 浏览器下载        ← C07 本地链路保留，不变
  └─ 同步开关开启时 → POST /api/photo（multipart，≤500KB）
        │
        ▼
服务器接收（不落服务器磁盘，直接内存转发）
  │  写 photo_sync 记录 state=received
  ▼
上传七牛 zz-1  ←→  失败重试（photo_sync.retry_count++）
  │  成功 → state=qiniu_ok（key: {网盘应用目录}/{参数目录}/{文件名}.jpg）
  ▼
后台推送百度（懒触发：新照片到达时尝试一次；否则积压等手动/定时）
  │  从七牛拉回内存 → BaiduPan 三步上传 /apps/{app_dir}/{参数目录}/{文件名}.jpg
  │  成功 → state=baidu_ok（终态，按生命周期策略清理七牛副本）
  ▼
网盘目录与手机相册镜像：/apps/{app_dir}/{sheet名称}/{小班号}/{相片文件名}.jpg
```

状态机（photo_sync.state，单向推进，每步幂等）：

```
received → qiniu_ok → baidu_ok(终态)
    └──────┴─ 失败留在当前态，retry_count++，last_error 记录
```

后台「同步状态」面板：按状态计数（received/qiniu_ok/baidu_ok），可按工作簿过滤，单张/批量手动重推。

## 4. 数据库设计（新增 2 表）

### settings（全局键值，基础设施配置，不进 git）

| 字段 | 类型 | 说明 |
|---|---|---|
| key | TEXT PK | 配置键 |
| value | TEXT | 配置值（secret 明文存 SQLite，data/ 目录已在服务器不进 git） |

预置键：`sync_enabled`（0/1，默认 0）、`sync_provider`（baidu，默认）、`sync_qiniu_ak`、`sync_qiniu_sk`、`sync_qiniu_bucket`（默认 zz-1）、`sync_qiniu_region`（默认 z1）、`sync_baidu_app_key`、`sync_baidu_secret_key`、`sync_baidu_app_dir`（默认 /apps/supervision_photos）、`sync_baidu_token`（JSON，含 refresh_token，可直接导入 trans_v2 的 .baidu_pan_token.json）、`sync_qiniu_keep_days`（默认 30）。

### photo_sync（每张照片一行）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | |
| workbook_id | INTEGER | 工作簿 |
| xiaoban | TEXT | 小班号（冗余，便于后台筛选） |
| filename | TEXT | 相片文件名（参数模板渲染结果） |
| remote_path | TEXT | 网盘目标路径（含 /apps/{app_dir} 前缀） |
| qiniu_key | TEXT | 七牛对象键 |
| size | INTEGER | 字节数 |
| state | TEXT | received / qiniu_ok / baidu_ok |
| retry_count | INTEGER | 当前态重试次数 |
| last_error | TEXT | 最近失败原因 |
| created_at | TEXT | 拍照时间 |
| synced_at | TEXT | 百度成功时间 |
| purged_at | TEXT | 七牛副本清理时间（生命周期策略） |

## 5. 后台「同步设置」UI（admin 新增卡片）

1. **同步开关**：默认关。开启前置校验：七牛 AK/SK/Bucket + 百度 app_key/secret/token 已导入，缺一不可。
2. **平台**：qiniu（默认，已实测）/ baidu（预留）。
3. **七牛配置**：AK / SK / Bucket（默认 zz-1）/ 区域（默认 z1）。
4. **百度配置**：app_key / secret_key / 网盘应用目录（默认 /apps/supervision_photos，与 trans_v2 的 book_translator 分开）。
5. **百度凭证**：app_key / secret_key / **token 导入**——后台提供导入框，直接粘贴 trans_v2 的 `.baidu_pan_token.json` 内容（含 refresh_token），保存后自动续期，**免 OAuth 授权页**。app_key/secret 必须与 token 签发应用一致（成套复制）。
6. **同步状态面板**：三态计数 + 最近失败列表 + 单张/批量重推 + 七牛副本保留天数。

## 6. 参数 sheet 新增 key（压缩参数下发，C01 参数驱动）

| key | 类型 | 默认值 | 说明 |
|---|---|---|---|
| 压缩最长边 | 数值 | 1600 | 手机端压缩，只影响新拍照片 |
| 压缩质量 | 数值 | 0.85 | JPEG 质量 |

说明：压缩参数走**参数 sheet**（随工作簿下发，每个工作簿可不同）；同步开关/云凭证属全局基础设施，走**后台 settings**。这是两类配置的边界——「业务跟模板走，基础设施跟后台走」。

## 7. API 设计

| 路由 | 方法 | 说明 |
|---|---|---|
| /api/photo | POST | 用户端拍照中转（multipart: file + workbook_id + filename + subdir）。开关关 → 423 明确提示；成功即返回，后续推送与手机无关 |
| /admin/api/sync/settings | GET/POST | 读写同步配置（secret 返回时打码） |
| /admin/api/sync/token | POST | 导入/替换百度 token JSON（成套校验 app_key 一致性） |
| /admin/api/sync/status | GET | 三态计数 + 最近失败 |
| /admin/api/sync/retry | POST | 手动重推（ids 或全部 cos_ok） |

推送触发：新照片 qiniu_ok 后立即尝试一次百度推送；失败留给积压，管理员手动重推（**不引入常驻定时进程**，保持 Flask 单进程轻量；将来量大再加 APScheduler）。

## 8. 里程碑（确认设计后排期）

- **M1 服务器接收 + 七牛**：settings 表 / photo_sync 表 / POST /api/photo（仅 App 调用）/ 七牛上传（真实凭证已在备份中，supervision settings 直接填入）/ 后台开关与配置页
- **M2 百度推送**：BaiduPan 移植 / token 导入（免授权页）/ 懒触发推送 + 手动重推 / 状态面板
- **M3 参数化收尾**：压缩参数 key 下发 / 七牛副本生命周期清理 / CONSTRAINTS.md 修订 C07

## 9. 开放问题（已全部确认）

1. ~~COS bucket 是否已有~~ → 改用七牛，bucket zz-1 已建，实测上传通过。
2. ~~百度授权方式~~ → 直接复制 trans_v2 的 token JSON + 凭证（成套），免授权页；实测 token 有效（剩 12.6 天，refresh_token 自动续期）。
3. ~~浏览器端是否同走中转~~ → 浏览器端**不拍照**，/api/photo 仅 App 端调用。

### 遗留提醒（实现 M1 时处理）
- trans_v2 `config.local.json` 丢失 cloud 段（见第 0 节警告），若其七牛功能仍需使用需自行恢复；supervision 的 settings 表填入备份中的真实 AK/SK 即可，不依赖 trans_v2 运行时。
