# AI 协作协议

> AI 每次会话开工前必读本文件 + CONSTRAINTS.md + BLOCKS.md（如有）。
> `[方括号]` 为占位符，新项目第一天替换为本项目实际内容。

## 指挥原则

1. 遇错先执行拿真实报错，不猜。
2. 改动方向：参数配置驱动优先、最小改动；边界：不改 [数据 sheet 内容与参数 sheet 语法约定] 只改代码。
3. 提交前对照原始数据自审，推导/默认值逻辑重点审。
4. >3 文件或数据结构变化，先出方案文档确认再动手。
5. 指令精确到三级坐标：积木编号 → 文件:行号 → 单元格/字段名，不说「改输出」。
6. 拿不准的业务判断列待确认清单（问题/候选方案/建议/依据），不替用户做决定。

## 项目铁律（违反=返工）

- 校验失败原样报错+列可选值，不自动映射/推导。
- 坏数据入口炸掉带行号，不静默跳过；所有记录被跳过时必须抛错，不生成空输出。
- 改算法前先读原始公式/数据/模板结构，证据先行，不凭记忆。
- 同一事实只算一次，跨表/跨处引用不重算；展示层能用公式用公式。
- 数值在源头一次取整，显示值=计算值，下游不再取整；表间勾稽在显示值上成立。
- 每次运行全新状态，全局缓存/配置入口重置，不跨运行残留。
- 改动后必须双跑全量用例对比（`python compare.py 旧/ 新/`），0 差异或预期差异才提交。
- 对比出差异先归因（代码bug/数据问题/基准错/合理多解），不默认改代码对齐。
- WARN 级问题汇总成报告文件，不逐个打断、不淹没控制台。
- 模板/生成物只填值不动样式；写入前先确认标签位/值位。

## 每次迭代必须

- 一版解决 1-3 个问题，版本号递增。
- 写 `doc/NNN-版本_主题.md`（模板见 doc/001，七段式：需求/验证/设计/代码/测试/反转/部署）。
- 新决策若推翻旧决策，写明反转链；同步更新 CONSTRAINTS.md。
- 改口径必同步三处：模板 + 代码 + 测试断言。

## 项目专属信息

- **业务一句话**：林业监督验收 App——把 Excel 网格数据表（如「中幼林抚育验收」）渲染为可填表页面，支持拍照（参数化命名/目录/水印）、GPS 打卡、小班轨迹记录与导出，功能由「参数」sheet 配置驱动。
- **输入**：Excel 模板工作簿（数据 sheet + 参数 sheet），示例 `data/中幼林抚育-网格填表模板.xlsx`；等级评定模板 `data/tpl-base.xlsx`（自 hqz-survey 迁移）。
- **输出**：Web/PWA 填表页面；照片按参数目录模板存相册；Excel 导出（管理后台）与轨迹 GPX 导出。
- **真实用例**：`data/中幼林抚育-网格填表模板.xlsx`（1174 行小班数据 + 8 键参数表）、`data/tpl-base.xlsx`、`data/tpl-samples.xlsx`。

## 参数 sheet 配置约定（本项目核心契约）

数据 sheet 的行为由「参数」sheet 驱动，**代码中禁止硬编码这些行为**（这是与 hqz-survey 最大的差异点）。规范详见 `doc/001-参数sheet配置规范.md`，要点：

- 表头固定 6 列：`key | value | 类型 | 默认值 | 说明 | 示例`
- 分隔符语法：多值 `;`；项内子字段 `|`；目录 `/`；文件名段 `_`；占位符 `{{列名}}`、`{{sheet名称}}`、`{{时间}}`；禁止全角标点
- 现有 8 键：相片备注、相片文件名、目录、验收结果选项、可编辑列、不显示列、功能、搜索选项
- **权威信息源排序**：[P0 客户 > P1 规范 > P2 案例 > …]
- **运行命令**：[如 python main.py data/xxx]
- **对比命令**：`python compare.py 旧输出目录/ 新输出目录/`
- **部署命令**：[如 bash deploy.sh]
