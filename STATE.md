# STATE — 当前状态快照

> 每次迭代后更新，≤25 行。跨会话恢复先读本文件，不翻 doc/ 归档。

- **版本**：v0.10.1 ｜ **更新**：2026-09-13（下午）
- **能跑**：`python main.py` → http://127.0.0.1:8720（雷华雄/lhx123 登录）：上传 Excel→列表选择→参数驱动网格填表→保存；后台 /admin 模板管理+下载 Excel+轨迹下载+验收变更日志+**相片云同步设置/状态面板**
- **v0.10.1 变化**：视频改为**手机端压缩为主**——「🎬 视频」按钮弹菜单：①录制（MediaRecorder 边录边压，720p/2.5Mbps/30fps/最长60s，全部后台可调，上传即压缩文件、服务器不再转码）②选择已有视频（服务器 ffmpeg 压缩，降级兜底）；新增 /api/video/params 只读参数接口与 video_phone_rec/fps/max_seconds 设置项；录制无声（APK 未声明 RECORD_AUDIO）；修复录制遮罩 flex 溢出致按钮不可点
- **v0.10 变化**：视频功能上线——详情页拍照与轨迹之间新增「🎬 视频」按钮（参数 sheet 功能 key 支持 `视频`）；视频经 `/api/video` 上传后由**服务器 ffmpeg 压缩**（后台「视频压缩」6 参数：最长边 720 / CRF 28 / 最大码率 2500kbps / 音频 96kbps / 单文件上限 300MB / ffmpeg 路径，实测 1080p 压到 ~9% 体积）；走七牛+百度同一链路，接收即返回、压缩推送后台异步；命名=相片文件名模板+`_视频`、目录同拍照；拍摄记录条目照片/视频样式区分（📷/灰绿 vs 🎬/蓝）；后台状态面板加类型与体积列；顺带修复百度建目录同名转存空副本问题（改先查再建）；线上工作簿已启用视频功能
- **v0.9.5 变化**：水印底块留白加大（内边距 ×0.7/×0.5、行距 ×1.45、圆角 ×0.35）+ 底块最小宽 30%；备注每行 22→26 字（减少换行）；超长备注自动缩字号（底块超画面 55% 时，下限 14px）保证完整入画；缓存熔断 v=0.8.7
- **v0.9.4 变化**：水印首行由「日期：YYYY-MM-DD」改为「时间：yyyy-MM-dd HH:mm:ss」（秒级完整时间戳，本地时区，C06 修订）；缓存熔断 v=0.8.6
- **v0.9.3 变化**：压缩优化——水印版式改「半透明白底块 + 实心黑字」（旧版白描边是高频边缘，费比特；实测同底图 0.80 编码省 7%，文字更清晰无彩边）；默认压缩参数 1600/0.85 → **1440/0.80**（约省 40% 体积，弱网上传时间减半）；模板参数 sheet 补「压缩最长边/压缩质量」两行；doc/001 更新为 10 键 + 水印版式规范；缓存熔断 v=0.8.5
- **v0.9.2 变化**：积压补推 + 七牛副本清理改后台 daemon 线程（拍照响应不受阻塞），purge_expired 单次限 20 个分批
- **v0.9 变化**：相片云同步上线（doc/007 实现）——App 拍照在开关开启时 POST /api/photo 中转：七牛 zz-1 暂存 + data/pending 缓冲 → 百度网盘 /apps/book_translator/supervision/{参数目录}/{文件名}.jpg（与手机相册 Pictures/ 镜像）；状态机 received→qiniu_ok→baidu_ok，百度成功删 pending、七牛副本按保留期（30 天）清理；后台同步设置卡（开关/凭证/token 导入/状态面板/手动重推）；参数 sheet 新增「压缩最长边/压缩质量」；纯标准库实现（服务器 shared_venv 无 requests/qiniu）；本地与线上均 curl 全链路验证 state=baidu_ok
- **已知问题**：v0.8 前上传的旧工作簿无 source 模板，导出需重新上传；轨迹记录不支持后台运行；v0.8.2 前已保存的行验收人为空；七牛 io 回读域名未开放（需绑定下载域名），当前回读兜底不可用——pending 缓冲为主回读源；百度 mkdir 对已存在目录偶发产生 `名字_时间戳` 副本目录（无害，生产文件名带时间戳不碰撞）
- **能跑**：https://forest.bibook.top/supervision 已上线 v0.9（2026-09-12 ./deploy.sh 部署；云凭证已配置、同步开关已开启；线上拍照实测 baidu_ok）；账号 雷华雄/lhx123
- **下一步**：1. [x] 相片云同步上线（doc/007） 2. [ ] GitHub Secrets 配置（KEYSTORE_BASE64/KEY_ALIAS/KEYSTORE_PASSWORD/KEY_PASSWORD）→ tag 触发 CI 出正式签名 APK 3. [ ] APP 备案（阿里云/域名备案主体入口，填包名 top.bibook.supervision + SHA256 指纹，见 android/keystore-info.txt）
- **最近迭代文档**：doc/007-相片云同步设计.md（云同步）、doc/001-参数sheet配置规范.md（10 键 + 水印版式 + 视频）
- **活跃约束**：C01–C09（详见 CONSTRAINTS.md）

## 资产活跃度（每 5 版评审时更新）

| 资产 | 上次发挥作用的版本 | 作用 | 处置 |
|---|---|---|---|
| compare.py | — | 尚未跑通 MVP | 观察 |
| CONSTRAINTS.md | v0.1 | 注册 C01/C02 | 保留 |

> 连续 5 版「未发挥作用」→ 按 README「资产进化」三选一：修复 / 合并 / 删除。
